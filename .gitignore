# Dependencies
node_modules
.pnpm-store

# Build
dist
build
*.tsbuildinfo
# tsc output of vite.config.ts — must never be committed, a stale
# vite.config.js would silently shadow vite.config.ts at dev/build time
vite.config.js
vite.config.d.ts

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Supabase
supabase/.branches
supabase/.temp

# local headless-verification tooling (not shipped)
.shot*.mjs
.texdump.mjs


# Asset pipeline: sources are re-downloadable CC0 originals, builds are staging
assets-src/
.assets-build/
