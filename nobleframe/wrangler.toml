# Cloudflare Pages — NobleFrame (inkl. OMEGA-OS-Showcase unter /showcase/omega-os/).
# Statische Seite = dieser Ordner. functions/ wird automatisch mitdeployed (Egregore-KI-Proxy).
name = "nobleframe"
pages_build_output_dir = "."
compatibility_date = "2024-09-01"

# Anthropic-Schlüssel NICHT hier ablegen, sondern als Secret:
#   Produktion: Pages > Settings > Environment variables > ANTHROPIC_API_KEY (Secret)
#   Lokal:      echo "ANTHROPIC_API_KEY=sk-ant-..." > .dev.vars
